{
    "views": {
      "por_id": {
        "map": "function(doc) { if (doc.type === 'tutor' && doc.id) { emit(doc.id, doc); }}"
      },
      "por_nombre": {
        "map": "function(doc) { if (doc.type === 'tutor' && doc.nombre) { emit(doc.nombre, doc); }}"
      }
    }
  }